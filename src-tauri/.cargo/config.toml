# Default build output for this crate: src-tauri/target.
# Note: a CARGO_TARGET_DIR environment variable takes precedence over this setting,
# which is why scripts/tauri-dev.js (`npm run tauri`) sets CARGO_TARGET_DIR explicitly.
[build]
target-dir = "target"
