{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. Least privilege (audit IPC-008): core:default minus core:event emit/emit_to (the frontend never emits, and a compromised webview could spoof backend events); no dialog permissions (the backend opens file dialogs itself and records the choice, IPC-001); opener limited to the Ollama link in the AI assistant; no reveal-in-folder.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:app:default",
    "core:image:default",
    "core:menu:default",
    "core:path:default",
    "core:resources:default",
    "core:tray:default",
    "core:webview:default",
    "core:window:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        {
          "url": "https://ollama.com/*"
        },
        {
          "url": "https://ollama.com"
        }
      ]
    },
    "updater:default",
    "process:allow-restart"
  ]
}
